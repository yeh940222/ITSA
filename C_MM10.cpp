#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
    double c;
    while (cin >> c) {
        cout << fixed << setprecision(1) << c * (9.0 / 5.0) + 32 << endl;
    }
    return 0;
}
