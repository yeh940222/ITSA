#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
    int mile;
    while (cin >> mile) {
        cout << fixed << setprecision(1) << round(mile * 16) / 10.0 << endl;
    }
    return 0;
}
